# NL Payroll Automation - UI Advanced (Fixed)

This build includes a fully working web UI with tabs and interactions.

Run:
```bash
cp .env.example .env
npm install
npm run dev
# open http://localhost:3000
```
